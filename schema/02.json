{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "waf-probe batch",
  "type": "object",
  "required": ["version", "count", "reports"],
  "properties": {
    "version": { "const": 1 },
    "count": { "type": "integer" },
    "reports": { "type": "array" }
  }
}